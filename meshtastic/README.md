# Meshtastic LoRa Experiments

Folder for **LoRa-based tactical communications** using Meshtastic devices.

## 🔹 Contents

- `device-configs/` – Firmware, channel, and device configuration files
- `pairing/` – Notes and instructions for pairing devices
- `gps-logs/` – Field GPS coverage mapping
- `experiments/` – Notes on signal range, battery life, and network stability

## 🔹 Usage

1. Flash devices with configs from `device-configs/`.
2. Refer to `pairing/` for node setup in the field.
3. Log GPS positions during tests in `gps-logs/`.
4. Document signal range, packet delivery rate, and anomalies in `experiments/`.

## 🔹 Notes

- Include photos or diagrams in `docs/` or `assets/` for reference.
- Track firmware versions for reproducibility.

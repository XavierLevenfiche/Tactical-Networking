# OpenWRT Mesh Configurations

Folder for **OpenWRT-based mesh networking experiments**, bridging devices, and custom automation scripts.

## 🔹 Contents

- `configs/` – Device configurations for mesh, Wi-Fi, and LAN/WAN
- `scripts/` – Automation for node monitoring, logging, and bridging
- `notes/` – Deployment observations, performance metrics
- `diagrams/` – Topology and wiring diagrams

## 🔹 Usage

1. Flash OpenWRT devices using configs from `configs/`.
2. Run scripts in `scripts/` for automation or monitoring.
3. Document performance and anomalies in `notes/`.
